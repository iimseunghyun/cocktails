<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.ish.mapper.AdminMapper">

<insert id="insert">
	INSERT INTO cocktails.members
	(mno, member_id, member_pass, name, isadmin)
	VALUES(null, #{member_id}, #{member_pass}, #{name}, 1);

</insert>

<select id="read" resultType="kr.ish.domain.MemberVO">
	select * from members where mno
	= #{mno}
</select>

</mapper>